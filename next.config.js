/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    unoptimized: true,
  },
  // experimental: {
  //   scrollRestoration: true,
  //   scrollBehavior: true,
  // },
};

module.exports = nextConfig;
